import { DeckLocator } from '@gamepark/react-game'

export class FoxEliteCardsDeckLocator extends DeckLocator {
  coordinates = {x: 25, y: -14}
}

export const foxEliteCardsDeckLocator = new FoxEliteCardsDeckLocator()
